# 実装計画

- [x] 1. オプションリスト定義の追加

  - `src/awsop/shell/wrapper.py`の`generate_shell_wrapper()`関数を更新
  - zsh スクリプト内にすべての CLI オプションを定義
  - 各オプションに短縮形、長形式、説明を含める
  - 形式: `'option:description'`
  - _要件: 1.1, 1.2, 1.4, 4.1, 4.2, 4.3_

- [x] 1.1 オプションリスト定義のユニットテストを作成

  - 生成されたスクリプトにすべてのオプションが含まれることを確認
  - 短縮形と長形式の両方が含まれることを確認
  - 説明が正しい形式であることを確認
  - _要件: 4.1, 4.2, 4.3_

- [x] 2. 文脈認識ロジックの実装

  - `_awsop()`補完関数に文脈解析ロジックを追加
  - 現在の単語（`$words[CURRENT]`）と前の単語（`$words[CURRENT-1]`）を取得
  - 現在の単語が`-`で始まる場合はオプション補完
  - 前の単語が`--source-profile`、`--output-profile`、`-p`、`-o`の場合はプロファイル補完
  - それ以外の場合はプロファイル補完
  - _要件: 3.1, 3.2, 3.4_

- [x] 2.1 プロパティテスト: オプション補完の排他性と完全性

  - **Feature: shell-option-completion, Property 1: オプション補完の排他性と完全性**
  - ランダムな`-`で始まる文字列を生成
  - 補完候補にすべてのオプションが含まれることを確認
  - 補完候補にプロファイル名が含まれないことを確認
  - 最小 100 回実行
  - **検証: 要件 1.1, 1.2, 3.1, 4.1, 4.2**

- [x] 2.2 プロパティテスト: プロファイル補完の排他性

  - **Feature: shell-option-completion, Property 2: プロファイル補完の排他性**
  - ランダムな`-`で始まらない文字列を生成
  - 補完候補にプロファイル名のみが含まれることを確認
  - 補完候補にオプションが含まれないことを確認
  - 最小 100 回実行
  - **検証: 要件 2.1, 3.2**

- [x] 2.3 プロパティテスト: プロファイル値補完

  - **Feature: shell-option-completion, Property 4: プロファイル値補完**
  - `--source-profile`、`--output-profile`、`-p`、`-o`の後の補完をテスト
  - 補完候補がプロファイル名のみであることを確認
  - 最小 100 回実行
  - **検証: 要件 2.3, 3.4**

- [x] 3. オプション説明の追加

  - すべてのオプションに日本語の説明を追加
  - 説明は簡潔で分かりやすいものにする
  - `_describe`関数で表示されるように形式を整える
  - _要件: 1.4, 6.1, 6.2_

- [x] 3.1 プロパティテスト: オプション説明の存在

  - **Feature: shell-option-completion, Property 3: オプション説明の存在**
  - 生成されたオプションリストを解析
  - すべてのオプションに説明が含まれることを確認
  - 形式が`option:description`であることを確認
  - 最小 100 回実行
  - **検証: 要件 1.4, 4.3, 6.1, 6.2**

- [x] 4. エラーハンドリングの実装

  - `~/.aws/config`ファイルが存在しない場合の処理を追加
  - エラーを発生させずに空のプロファイルリストを返す
  - 既存のエラーハンドリングを保持
  - _要件: 2.5_

- [x] 4.1 プロパティテスト: エラー耐性

  - **Feature: shell-option-completion, Property 6: エラー耐性**
  - 一時的に`~/.aws/config`を削除または移動
  - 補完関数がエラーなく実行されることを確認
  - 空のプロファイルリストが返されることを確認
  - 最小 100 回実行
  - **検証: 要件 2.5**

- [x] 5. 後方互換性の確認

  - `awsop()`ラッパー関数のロジックを変更しない
  - 関数名（`awsop()`, `_awsop()`）を変更しない
  - 既存のプロファイル補完動作を保持
  - _要件: 7.1, 7.2, 7.3, 7.4_

- [x] 5.1 後方互換性のユニットテスト

  - 生成されたスクリプトに`awsop()`関数が含まれることを確認
  - 生成されたスクリプトに`_awsop()`関数が含まれることを確認
  - `awsop()`関数のロジックが変更されていないことを確認
  - zsh 構文チェック（`zsh -n`）を実行
  - _要件: 7.1, 7.2, 7.3, 7.4_

- [x] 6. チェックポイント - すべてのテストが通ることを確認

  - すべてのテストが通ることを確認し、問題があればユーザーに質問する

- [x] 7. ドキュメントの更新
  - README に新機能の説明を追加
  - `--init-shell`の再実行手順を追加
  - CHANGELOG に変更内容を記載
  - _要件: なし（ドキュメント更新）_
